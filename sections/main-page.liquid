<section class="py-12">
  <div class="page-width max-w-3xl">
    <h1 class="text-3xl font-bold mb-8" style="font-family: var(--font-heading);">{{ page.title }}</h1>

    <div class="prose prose-lg max-w-none">
      {{ page.content }}
    </div>
  </div>
</section>

{% schema %}
{
  "name": "Page",
  "tag": "section"
}
{% endschema %}
